<div class="page-header">
  <h1>All Statuses</h1>
</div>

<%= link_to "Post a New Status", new_status_path, class:"btn btn-success" %>

<% @statuses.each do |status| %>

<% if false %>
where do @statuses point to? the "statuses" variable created in db migrate? 
<% end %>

<div class="status">
  <strong><%= status.name %></strong>
  <p><%= status.content %></p>


  <div class="meta">
    <%= link_to time_ago_in_words(status.created_at)+ " ago", status %> | 

    <% if false %>
    how does this work. Is "show" already linked to show.html.erb? and what does "status" do? "show" seems to just be text to be printed. thus "status" is the path. Where does it lead.

    Changing to "time_ago_in_words" (from api ruby docs), how do we define status.created_at? is .created_at already a defined method?
    <% end %>

    <span class="admin">
      <%= link_to "Edit", edit_status_path(status) %> | 
      <%= link_to "Delete", status, method: :delete, data: { confirm: "Are you sure you want to delete this status?" } %>

      <% if false %>
        m
      <% end %>
      
    </span>
  </div>
</div>
<% end %>
